from django.apps import AppConfig


class PrometheusScraperConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.prometheus_scraper"
    verbose_name = "Prometheus Scraper"
