apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: my-service
    component: api
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 44926
      protocol: TCP
      name: http
  selector:
    app: my-service
    component: api
